<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.finsky.permission.BIND_GET_INSTALL_REFERRER_SERVICE" />

    <application>

        <provider
            android:name="ads.popbrain.sdk.SdkInitProvider"
            android:authorities="${applicationId}.popbrainadsdkinit"
            android:exported="false" />

        <!--
            Restricted to senders holding INSTALL_PACKAGES (app stores). Without this any
            app on the device could broadcast a forged referrer and claim credit for an
            organic install.
        -->
        <receiver
            android:name="ads.popbrain.sdk.InstallReferrerReceiver"
            android:exported="true"
            android:permission="android.permission.INSTALL_PACKAGES">
            <intent-filter>
                <action android:name="com.android.vending.INSTALL_REFERRER" />
            </intent-filter>
        </receiver>

    </application>

</manifest>